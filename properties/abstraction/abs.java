package practice.Object_oriented_programming.properties.abstraction;

import java.util.ArrayList;

public class abs {
    public static void main(String[] args) {
        ArrayList<Integer> A1 = new ArrayList<>();
        /**the above thing is the abstracted class that means we don't care how the arraylist store or print the value that is presentvin it
         * al though we can just control enter over it know more but we don't wanna know
         *
         *
         * this is what abstraction is all about
         *
         * abstaction is means wrapping of uneccessry details from the user like that above the user don't wanna know how internally thing work
         * how they store in memory but still he can able to use it this is what abstrcation all about
         *
         *
         *
         * encapsulation means hide the data or urbind the data that the user can be used **/
    }
}
